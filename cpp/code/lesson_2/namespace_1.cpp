#include <iostream>

int main()
{
    using namespace std;

    cout << "we are using std namespace, so invoke cout and endl,  " << endl;
    cout << "so we can insert text into console by just using cout instead of using std::cout" << endl;

    return 0;
}